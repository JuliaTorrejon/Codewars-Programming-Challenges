def count_positives_sum_negatives(arr):
    if not arr: return []
    count_positives = 0
    count_negatives = 0
    for n in arr:
      if n > 0:
          positive += 1
      if n < 0:
          negative += n
    return [count_positives, sum_negatives]
    
